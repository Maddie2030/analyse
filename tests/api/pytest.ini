[pytest]
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -ra
markers =
    external: requires a real external scraper source URL
    journey: real user/admin actor journey with intended state assertions
    worker: requires background worker containers to be running
    write: writes only isolated pytest-owned test data

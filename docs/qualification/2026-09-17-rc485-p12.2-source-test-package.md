# RC4.85 P12.2 Source-Test / Deployment Package

Date: 2026-09-17
Branch: `sequential/p06.4-caller-cutover`
Packaged Git tree: `8f7c169c8e385fae578b2e198ec75da56e610585`
P11.4 source/test repair: `ed53fc51c96b299427781f4b2564911accba7c97`
P11.3 closure ancestor: `00e35924ca5dfe34358c4348cea43f656e7a230e`
Forensic stash: `ffbcfe842c0e7f0910eae8ccf4fe64f822c01af4` — not applied or popped.

## Artifact

- File: `mreader-rc485-p12.2-source-test-8f7c169.zip`
- SHA-256: `614cc7a9cb5eff158264b6f32c7357fbe9eb72935777bc1d2c6efe6b390a795f`
- Exact committed-tree files: `932`
- Package files after generated package metadata/evidence: `937`
- `PACKAGE-CHECKSUMS.sha256` entries: `936` (every package file except the checksum manifest itself)

This is a full application source-test/deployment checkpoint. It is not a compiled APK and is not a final release-qualified RC4.85 package. Runtime labels intentionally remain `1.3.0-rc4.84` until P12.3 final qualification.

## Fresh package qualification

The package was built from `git archive HEAD`, so application/source files come only from the exact clean committed tree at `8f7c169`.

Fresh checks:

1. `git status --short --branch` — clean `sequential/p06.4-caller-cutover` before packaging.
2. `git diff --check` — PASS.
3. `bash scripts/validate-current-release.sh` — PASS on its available dependency-light/source/static path. The validator explicitly skips Docker Compose render because Docker and a configured `.env` are unavailable in this sandbox; that skip is not runtime acceptance.
4. Re-opened ZIP + `sha256sum -c PACKAGE-CHECKSUMS.sha256` — all 936 entries PASS.
5. Exact-tree comparison — all 932 committed tree files in the package are byte-identical to a fresh `git archive 8f7c169`; zero differences.
6. Deterministic rebuild — rebuilding the ZIP from the same staged bytes and normalized commit timestamp produces byte-identical ZIP output.
7. ZIP digest — `614cc7a9cb5eff158264b6f32c7357fbe9eb72935777bc1d2c6efe6b390a795f`.

The package adds only generated package metadata/evidence files on top of the exact committed tree: `PACKAGE-START-HERE.md`, `PACKAGE-SOURCE-CHECKPOINT.json`, `PACKAGE-TEST-STATUS.md`, `PACKAGE-CHECKSUMS.sha256`, and `package-evidence/validate-current-release.log`.

## Acceptance status carried into the package

P11.4 dependency-light/source review remains green at `ed53fc5`: Web reading 31/31; Android source 8/8 plus full static audit; migration/quiescence/reconciliation/pre-upgrade 21/21; P09 source fences 34/34 + 28/28; P10 truthfulness/race fences 50/50; API ownership and 139/139 route classification; resource/pressure guards; current-release validator source/static path PASS.

Release acceptance remains blocked:

- P11.1 same-checkpoint builds/runtime/API qualification — **BLOCKED**;
- P11.2 destructive upgrade/restore rehearsal on real disposable Docker/PostgreSQL/Kubernetes — **BLOCKED**;
- P11.3 real PostgreSQL plans/load/KEDA-HPA/multi-replica cache qualification — **BLOCKED**;
- P11.4 genuinely independent Web/Android/migration review — **BLOCKED**.

No blocked or skipped gate is represented as passed.

## Intended use

Use this package on the real MReader Windows/Git Bash + Docker Desktop/Kubernetes host to execute the blocked runtime gates against disposable/test data first. Follow `PACKAGE-START-HERE.md` and the repository `README.md`. Do not use the only copy of production/user data for destructive restore qualification.

The earlier Milestone-1 source-test package remains preserved and is not overwritten by P12.2.

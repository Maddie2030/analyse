# RC4.85 P03.2 browser acceptance source coverage

Date: 2026-09-17

Source checkpoint: `d7fd2948f64920a8851780e64d94de6a795c6c7a`

## Result

The previously missing P03.2 browser acceptance source coverage now exists in `tests/browser/reading-consistency.spec.mjs` and is enforced by `tests/regression/browser-reading-acceptance-static.sh` through the supported current-release validator. No production Web reading behavior changed in this unit.

The Playwright source covers five required browser scenarios against the real Reader/Progress interfaces:

1. two tabs share durable IndexedDB state while one tab cannot sync;
2. IndexedDB quota failure keeps intent visible and prevents unsafe transport;
3. an offline refresh cycle preserves the pending checkpoint until connectivity returns;
4. a delayed Progress acknowledgement cannot erase a newer checkpoint produced during transport;
5. a protected-media failure cannot fabricate chapter completion.

## Fresh local evidence

- `bash tests/regression/browser-reading-acceptance-static.sh` — PASS.
- `bash tests/regression/progress-consistency-static.sh` — 31/31 PASS.
- `node --check tests/browser/*.mjs` — PASS.
- `bash scripts/validate-current-release.sh` — PASS on its source/static path; Docker Compose render remains skipped because Docker/.env are unavailable here.
- `git diff --check` — PASS.
- Ripwire test-gate identified the new browser acceptance contract as the direct obligation; that obligation was run green.

## Still open

P03.2 is **not accepted** by this source milestone. Real Playwright execution still requires the deployed Docker/browser/PostgreSQL topology, and independent scoped review still requires a genuinely separate reviewer/subagent. Those remain blockers for LOCAL-01/02/03, READ-03/04, UI-01 and the P11/P12 release gates.

P03.2 package authority: `mreader-rc485-p12.2-source-test-d7fd294.zip`, exact source `d7fd2948f64920a8851780e64d94de6a795c6c7a`, SHA-256 `12e37a434d9f2cd2e824d4ebee174563da57ba23b091c18b0df9c4ae25e22e51`; 957/957 committed files exact, 959/959 package checksum entries, deterministic rebuild byte-identical. Real Playwright execution and independent review remain open.

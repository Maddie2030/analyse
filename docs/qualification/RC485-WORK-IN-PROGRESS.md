# RC4.85 implementation checkpoint — not a release

This checkout is an unfinished implementation of the approved ownership consolidation. Original uploaded source archives remain unchanged. No live deployment, database, or production restore has been modified or executed.

## Implemented source and remaining reading work

- Host-local PostgreSQL protection has one configured root, recovery catalog and operation queue. Real dump/snapshot restore qualification and secure local downloads remain outstanding.
- Progress has a draft transactional command path for resume, exact chapter evidence and outbox effects, with session/sequence/revision ordering. Normal runtime no longer acknowledges a Redis write-behind update.
- Web has a shared IndexedDB reading repository. The account-transition, delayed-restore and primary-action fix wave passed 31 focused Node behavioral tests. Independent scoped re-review remains pending after reviewer quota interruptions; browser/build qualification is still blocked.
- Android source now uses one account/origin-scoped journal and the shared open/session/sequence API. The revision-only adapter and independent recent-read owner are removed. Local storage failure, conflict retention, exact-chapter restoration, media completion evidence, pending Continue actions and the canonical Recently Opened envelope are wired. Eleven focused Kotlin behavioral tests are written; neither those tests nor the Android application have been compiled or run here. Source checks passed; independent task review remains pending.
- Backend changed-payload clamping, migrated checkpoint retention, bounded identity documentation and account-header fencing fixes passed scoped source re-review. Their real API and migration regressions remain blocked pending PostgreSQL and build tooling.
- Upgrade and restore candidates now share one migration executor and lock. Transactional preservation precedes pending 047/048, and 052 separates inferred furthest reach from exact history/recency. Social consumes that reach for unread calculations; actual opens and the explicit maintenance drain replace inferred timestamps with event timestamps. Six shell-runner cases pass. The ten actual PostgreSQL cases, added API regression and independent source review remain blocked/pending; see `RC485-READING-UPGRADE.md`.

- Sequential P05 re-review added migration-time Kubernetes quiescence and explicit legacy Progress drain before preservation/migration. A forward-only 053 repair prevents an unproven legacy queued backup request from being replayed after migration 050; scraper-history retirement remains deferred to P07 where the canonical ingestion header exists. Source checks pass, while real PostgreSQL/Kubernetes/Docker qualification remains blocked. See `RC485-P05-MIGRATION-SOURCE-REVIEW.md`.

- Sequential P06 promoted the independently reviewed `17c28ac` publication contract and implemented the P06.3 durability boundary at `5c36831`. The reopened gate pulled forward only the required P07.1 authority prerequisite: migration 055 adds the canonical `ingestion_operations` header, and Catalog verifies retained actor, current admin authorization, exact source revision, exact ingestion generation and cancellation/running state before any new publication mutation. `2a9ff5c` additionally requires a durable receipt replay to recheck the actor's current active-admin authorization while intentionally bypassing later operation-state fences after a proven commit. `2dbc64d` repairs the Docker execution path so the read-only repository mount is copied into writable container scratch before `go mod tidy`, `go mod verify`, and the focused transaction suite; `ffc166f` pins the official Go 1.25.0/Alpine 3.22 image index to `sha256:f18a072054848d87a8077455f0ac8a25886f2397f88bfdd222d6fafbb5bba440`. A separate concurrent-create DB case is present. Private workload credential authentication remains P06.5/P09.1 scope. Commit `ad0aa0a` adds revision-bearing `chapter.published` v2 and version-safe Notification dedupe; `78b5cf8` adds the real PostgreSQL same-revision/next-revision/legacy-v1 Notification dedupe case. The focused P06.3 source suite is 60/60, migration-runner checks are 6/6, Catalog dependency-free Go contract checks are 3/3, Notification event Go checks are 2/2, the gate harness is 8/8, and Ripwire reports zero gating quality regressions/zero untested blast radius for the new evidence slice. Docker mode supplies PostgreSQL 16.10 and Go 1.25 but is not offline: pinned images and Catalog Go modules must be cached or downloadable. This sandbox has no Docker daemon, so the real transaction suite remains unexecuted. P06.4 remains paused; see `RC485-P06.3-COMPLETION-GATE.md`.

- Recovery operator paths now resolve the same home-directory root before Docker. Startup validation checks the requested 4/2 defaults, and the one-time environment migration preserves custom settings and the original configuration. The obsolete NAS storage-doctor path delegates to the existing local owner. Eight behavioral operator fixtures and the updated Bash entry-point regressions pass; no live backup, replication repair or restore was executed.

## Actual qualification limits

The current environment has Node, Java, Python and Go 1.23.2/gofmt. The P06.3 Docker gate no longer depends on host Go/PostgreSQL because it supplies Go 1.25 and PostgreSQL 16.10 itself, but Docker is unavailable in this sandbox and no disposable external PostgreSQL DSN is configured. A Docker-host run also needs the pinned images and Catalog Go modules available from cache or reachable registries/proxies. Other previously recorded Android/browser dependency limits still apply unless a task-specific probe says otherwise. Real service, migration, restore, browser and Android runtime gates are **blocked**, not passing. Python/source/static checks and Ripwire structural evidence are not runtime integration evidence.

## Combined-release work still required

1. Finish and review the reading contract on both clients, including shared-cookie account fencing, storage failure, completion evidence, stale responses and exact-chapter resume.
2. Review and qualify pre-047/048 preservation against actual PostgreSQL, all supplied versions, fresh installs and partial ledgers. Complete writer quiescence and legacy-stream drain ordering before destructive steps. Do not rely on a later migration to recover data already deleted.
3. Complete Catalog publication ownership, ingestion operation/receipt integration, cancellation and exact-object cleanup; then enforce narrow runtime database grants.
4. Restore authenticated recovery downloads through a private transport from the host-local owner, first narrowing the environment currently copied into both Kubernetes namespaces. Qualify full logical/physical restore, retention pins, generation fencing and operator/catalog recovery alignment against isolated PostgreSQL.
5. Align route/client/load fixtures and execute the release gates in the approved specification, including capability and resource-budget checks.

The user's 2026-09-14 instruction authorizes full application source test packages when a major blocker is resolved. Milestone 1 addresses the obsolete recovery startup guards; the 13 validator source/fixture regressions and production backup-policy assertions pass. This authorization does not turn blocked runtime checks into passes. No final release-version bump or deployment qualification is claimed. `00-START-HERE.md` documents the test package and remaining work.

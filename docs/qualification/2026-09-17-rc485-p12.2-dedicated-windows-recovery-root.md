# RC4.85 P12.2 dedicated Windows database-protection root

Date: 2026-09-17
Branch: `sequential/p06.4-caller-cutover`
Source repair checkpoint: `1a682bb0a7d96af50fecb54cb77cbf13876dadcb`
Previous Git Bash hardening source: `1d17ce50604001f8e6462c8492ccc9582353f4a5`
Forensic stash: `ffbcfe842c0e7f0910eae8ccf4fe64f822c01af4` — untouched.

## Real-host failure

Windows/Git Bash deployment repeatedly reached:

```text
hybrid pre-upgrade backup ordering regression PASSED (backup line 25 < migration line 48)
hybrid PostgreSQL volume inspection static regression PASSED
Created .env from .env.example
ERROR: Use an absolute Windows drive path, such as C:/Users/name/.mreader/database-protection.
```

The preceding profile-shape repairs still depended on discovering a usable Windows home/profile when a fresh `.env` contained no explicit database-protection root. The operator requirement is simpler: use one stable dedicated Windows host directory for this mount.

## Repair

`1a682bb0a7d96af50fecb54cb77cbf13876dadcb` changes the blank Windows default in the shared database-protection resolver to:

```text
C:/mreader/database-protection
```

Bootstrap persists that literal value into `.env` and creates the directory before stateful adoption/preflight. Existing valid explicit `MREADER_DB_PROTECTION_ROOT` values are still preserved and take precedence; Linux defaults remain host-home based.

All existing startup/backup/restore/readiness callers continue to consume the one shared resolver rather than duplicating Windows path logic.

## TDD evidence

The new tests were retained while the production resolver/bootstrap changes were temporarily restored to the previous committed state. Both failed as expected:

- `tests.regression.test_db_protection_root` — failed because the previous resolver returned profile-derived `C:/Users/.../.mreader/database-protection` rather than the dedicated root;
- `tests/regression/bootstrap-windows-dedicated-root.sh` — failed with the exact real-host message `ERROR: Use an absolute Windows drive path...` after creating `.env`.

After restoring the repair, both became GREEN.

## Fresh committed-tree evidence

- recovery-root unit/persistence suite: **18/18 PASS**;
- fresh Windows/Git-Bash dedicated-root bootstrap regression: PASS;
- existing explicit-root bootstrap regression: PASS;
- local pre-upgrade backup: **5/5 PASS**;
- local database-protection deployment: **4/4 PASS**;
- hybrid pre-upgrade ordering: PASS;
- PostgreSQL volume inspection: PASS;
- explicit volume ownership/adoption: PASS;
- no-host-Python regression: PASS;
- MSYS container-path contract: PASS;
- storage env static + migration runtime contracts: PASS;
- ownership route audit: **139/139 PASS**;
- PostgreSQL role audit: **15 capabilities / 18 workload identities PASS**;
- current-release validator: PASS on available dependency-light/source/static path;
- `git diff --check`: PASS before source commit.

Docker Compose rendering remains BLOCKED/SKIPPED in this sandbox because Docker and the machine-specific runtime `.env` are unavailable. This does not change any P11 release-acceptance blocker to PASS.

## Replacement P12.2 package

- Artifact: `mreader-rc485-p12.2-source-test-1a682bb.zip`
- Packaged Git tree: `1a682bb0a7d96af50fecb54cb77cbf13876dadcb`
- ZIP SHA-256: `1545a8eac36af2dc6e9b0fede7894e082c7efabc2cb8244e2b08a378254039f6`
- Committed source files: **938**
- Package checksum entries: **940**
- Every committed source file matches fresh `git archive 1a682bb` bytes.
- Reopened package checksum verification: **940/940 PASS**.
- Package-extracted dedicated-root/bootstrap/pre-upgrade/volume/no-host-Python/ownership/role checks: PASS.
- Deterministic rebuild: byte-identical.

The prior `1d17ce5` package remains historical evidence but is superseded for Windows deployment testing by this package.

## Remaining release blockers

P11.1 build/runtime/API acceptance, P11.2 destructive restore rehearsal, P11.3 live performance/KEDA-HPA/cache qualification, and P11.4 genuinely independent review remain BLOCKED.

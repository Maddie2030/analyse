# RC4.85 P11.2 Upgrade / Restore Rehearsal Qualification

Date: 2026-09-17
Canonical branch: `sequential/p06.4-caller-cutover`
Checkpoint under test: `75df6a840c44acf2c409e846419ed8c7a062c796`
P10.2 durable ancestor: `65f0c2ebada66a653700e76025d2a5ba1a051f69`

## Result

P11.2 is **BLOCKED for release acceptance** in this sandbox because the actual Docker Desktop / PostgreSQL / Kubernetes deployment topology is unavailable. The user explicitly authorized continuing past runtime blockers for sequencing only. This waiver does not convert any blocked restore rehearsal into a pass.

No production source was changed by P11.2 qualification.

## Source-version / recovery fixture evidence

The migration, quiescence, pre-upgrade capture, recovery-catalog, private recovery, restore-fencing, generation-readiness, and operator-alignment Python suite ran from the exact checkpoint:

- `123` tests executed
- `0` failures
- `10` skips
- all 10 skips are the real PostgreSQL migration cases requiring both `psql` and an explicit disposable `MREADER_TEST_POSTGRES_DSN`

The dependency-light shell recovery layer additionally passed:

- backup-agent static contract
- database operation ledger runtime fixture
- database protection runtime/static contracts
- database restore/recovery static contract
- RC4.84 upgrade/recovery contract
- snapshot restore static contract
- snapshot archive/runtime contract
- legacy volume authority / priority
- pre-upgrade backup ordering
- PostgreSQL volume inspection
- stateful volume adoption / ownership
- storage doctor fixture
- storage env contract / migration
- staging portability / permissions
- NAS running-deployment adoption

## Inherited fixture blockers

Two shell fixtures do not complete in this sandbox and are not treated as passing:

1. `tests/regression/backup-daily-policy.sh` hangs under the sandbox harness.
2. `tests/regression/backup-local-runtime.sh` times out while invoking the fake-environment `backup-agent.sh manual runtime-test` path. `bash -x` confirms the timeout occurs inside that invocation before the fixture reaches its assertions.

These are recorded as blocked fixture execution, not application success.

## Production entrypoint probes

A detached qualification worktree at exact checkpoint `75df6a8` was given a synthetic, non-production `.env` and isolated recovery root under `/mnt/data/mreader-p112-recovery-root`. The following real entrypoints were then probed without any live target:

- `postgres-restore.sh bkp_000000000000000000000000 --yes` → stops at missing Docker before resolving a verified recovery point.
- `postgres-restore-drill.sh --backup-id bkp_000000000000000000000000` → `ERROR: docker is required.`
- `catalog-restore.sh --backup-id bkp_000000000000000000000000 --check` → `ERROR: docker is required.`
- `postgres-backup.sh status` → Docker command unavailable.
- `create-local-preupgrade-backup.sh .env` → `ERROR: docker is required for local pre-upgrade backup.`

No PostgreSQL, Docker, Kubernetes, API, NAS, or user-data mutation occurred.

## Acceptance debt that remains open

P11.2 still requires, in an explicitly authorized disposable environment using the actual deployment topology:

- RC4.81 / supplied RC4.84 / partial-upgrade / fresh-install migration convergence with real PostgreSQL;
- verified pre-upgrade dump and physical snapshot capture;
- isolated dump and snapshot drill restore;
- production-restore rehearsal with writer/autoscaler fencing;
- interruption injection before/after destructive rename/journal stages;
- generation reconciliation and stale work/event replay checks after recovery/rollback;
- real Docker/Kubernetes/NAS integration where applicable.

## Sequencing waiver

The user authorized `skip the blocker` on 2026-09-17. Therefore subsequent P11 lanes may be explored and source-qualified, but final release acceptance must continue to list P11.1 and P11.2 runtime gates as BLOCKED until rerun in an authorized environment.

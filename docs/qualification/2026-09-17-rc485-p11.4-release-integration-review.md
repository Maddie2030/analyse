# RC4.85 P11.4 Independent Source / Release Integration Review

Date: 2026-09-17
Branch: `sequential/p06.4-caller-cutover`
Starting durable checkpoint: `00e35924ca5dfe34358c4348cea43f656e7a230e`
P11.4 source/test checkpoint: `ed53fc51c96b299427781f4b2564911accba7c97`
Forensic stash: `ffbcfe842c0e7f0910eae8ccf4fe64f822c01af4` — not applied or popped.

## Result

P11.4's **available source/release-integration review is complete**, but P11.4 is **BLOCKED for final independent-review acceptance** in this sandbox.

The current-release validator and all dependency-light review scopes available here pass after two stale regression assertions were corrected. Neither correction changes production application behavior. A genuinely separate reviewer/subagent is not available in this runtime, so the Web/Android/migration review cannot be self-certified as independent. P11.1, P11.2 and P11.3 runtime blockers also remain release blockers; the earlier sequencing waiver does not convert them into passes.

RC4.85 is therefore **not release-qualified** by this report.

## P11.4 findings and minimal repairs

### 1. Android static audit carried a stale Smart Library SQL text expectation

`services/social_ts/src/routes.ts` already consumes the Progress-owned `reading_state_v1` projection and keeps the query user-scoped. The static audit still searched for the old contiguous text `FROM reading_state_v1 WHERE`, which no longer matched the formatted/current query shape.

Repair in `scripts/android-static-audit.sh`:

- assert `SELECT * FROM reading_state_v1` for projection ownership;
- assert the explicit user-scoped History predicate `WHERE user_id=$1::uuid AND (has_history OR furthest_chapter_id IS NOT NULL)`.

RED→GREEN proof:

- old assertion: `scripts/android-static-audit.sh` exits 1 with `Smart Library consumes the Progress-owned reading projection ... not found`;
- repaired assertion: the full Android static integration audit passes.

### 2. Dependency pin regression still required retired `curl`

P08.3 replaced the old curl-based private recovery bridge path with the in-image Go bridge. `ops/postgres-backup/Dockerfile` intentionally no longer installs `curl`, but `tests/regression/dependency-pins.sh` still expected it.

Repair in `tests/regression/dependency-pins.sh`:

- assert the exact current Alpine install line with `bash`, `jq`, `coreutils`, and `tzdata` only;
- keep the existing digest pin assertions unchanged.

RED→GREEN proof:

- old assertion: dependency-pin gate exits 1 with `postgres backup Alpine package pins drifted`;
- repaired assertion: `dependency pin regression PASSED`.

No production Dockerfile, service, API, migration, resource limit, route, grant, or runtime behavior changed in P11.4.

## Fresh source/release integration evidence

| Scope | Result | Evidence |
| --- | --- | --- |
| Web reading/repository review | PASS | `node --experimental-strip-types --test tests/regression/test_web_reading_repository.mjs` → 31/31 |
| Android reading/origin source review | PASS | `python3 -m unittest -v tests.regression.test_rc485_p04_android_source` → 8/8 |
| Android full static integration audit | PASS | `bash scripts/android-static-audit.sh` |
| Web/Android reader contract | PASS | `bash scripts/android-web-reader-contract-audit.sh` |
| Migration/quiescence/reconciliation/pre-upgrade source review | PASS | 21/21 across `test_migration_quiescence`, `test_migration_runner`, `test_legacy_operation_reconciliation`, `test_local_preupgrade_backup` |
| Pre-upgrade ordering | PASS | `bash tests/regression/hybrid-preupgrade-backup-order.sh` |
| P09.2/P09.3 ownership/grant/runtime-DSN source fences | PASS | 34/34 |
| P09.4/P09.5 readiness/route-consumer source fences | PASS | 28/28 |
| API ownership | PASS | `bash tests/regression/api-flow-ownership-static.sh` |
| API route coverage | PASS | 139 source routes classified |
| P10.1/P10.2 truthfulness/race source fences | PASS | 50/50 with `PYTHONPATH=tests/regression` |
| Resource/pressure budgets | PASS | pressure-efficiency, user-plane resource budget, twin-plane resource budget |
| Dependency pins | PASS | direct gate and RTK execution both pass |
| Browser spec syntax | PASS | every `tests/browser/*.mjs` passes `node --check` |
| Current release validator | PASS, source/static path | `bash scripts/validate-current-release.sh` exits 0; Docker Compose render explicitly SKIP because Docker + configured `.env` are unavailable |
| Diff/shell syntax | PASS | `git diff --check`; `bash -n` on changed shell gates |

The current-release validator reached `MReader 1.3.0-rc4.84 current release validation PASSED`. Its Docker Compose render branch printed `SKIP: Docker Compose config render requires Docker and a configured .env.` That skip is not runtime acceptance.

## Ripwire / Drushti evidence

- `Drushti doctor` reports Superpowers active and Ripwire/Caveman/Headroom/RTK present.
- Ripwire `--situ` over the two changed test files reports 0 impacted symbols and 0 transitive blast-radius symbols, while disclosing shell-script modeling limits.
- Ripwire `--test-gate` reports `untested=0` and one changed-test obligation: `tests/regression/dependency-pins.sh`. That exact gate was executed successfully. Exit 4 is therefore the expected obligation signal, not a failing assertion.
- Caveman executed the changed-tree `git diff --check`; its native independent agent integrations are unavailable in this sandbox.
- Headroom local memory/status is available. Structural diff/repo-shape helpers are blocked because their local `difftastic`/`scc` binaries are absent and outbound DNS cannot fetch them.
- RTK compatibility mode successfully executed `tests/regression/dependency-pins.sh`.

## Runtime/dependency identity

Available:

- Git 2.47.3
- Python 3.13.5
- Node 22.16.0 / npm 10.9.2
- Go 1.23.2
- OpenJDK 21.0.11
- Ripwire 0.6.1
- Headroom 0.37.0
- RTK compatibility launcher for source contract 0.48.0

Unavailable for required acceptance gates:

- Docker
- kubectl
- psql
- k6
- frontend `node_modules`
- Android Gradle wrapper JAR
- Python runtime dependencies `psycopg`, `asyncpg`, `selectolax`
- required Go 1.25 toolchain

## Independent-review blocker

The tracker requires a genuinely independent review and explicitly forbids recording self-review as independent. `caveman status` reports the native Claude/Codex/Hermes/Gemini/OpenCode/Pi/Aider integrations unavailable. No separate reviewer/subagent surface is available in this sandbox.

Therefore:

- Web P03.2 independent review: **BLOCKED**;
- Android P04.2 independent review: **BLOCKED**;
- migration P05.5 independent/runtime review: **BLOCKED**;
- P11.4 independent-review acceptance: **BLOCKED**.

The dependency-light source evidence above is useful review evidence but does not satisfy the independent-review requirement.

## Release blockers carried forward

P11.1 remains BLOCKED for same-checkpoint build/runtime acceptance. P11.2 remains BLOCKED for destructive Docker/PostgreSQL/Kubernetes restore rehearsal. P11.3 remains BLOCKED for real PostgreSQL query plans, live concurrency/load budgets, KEDA/HPA and multi-replica cache behavior. P11.4 remains BLOCKED on independent reviewer availability.

Do not begin P12.3/final qualified release packaging until those required gates are executed successfully against the exact integrated checkpoint.

# RC4.85 P12.2 — Windows drive-form Python host-input repair

## Failure

Real Windows/Git-Bash execution reported:

`hybrid host dependency regression FAILED: Windows drive-form host input was not bind-mounted into the Docker Python runtime`

The prior regression fixture created `Z:/...` as a repository-relative filename. That works on Linux, but Git Bash interprets it as a real `Z:` drive, so the fixture's existence check diverged by platform. The shared Docker-Python fallback also relied directly on `-e "$arg"` for drive-form paths.

## Repair

Source `b80c030327e16a9e05dfd7b979a5b5ee668ce2de`:

- resolves Windows drive-form arguments through `cygpath -u` before host existence checks;
- bind-mounts confirmed host inputs read-only and rewrites Python arguments to `/mreader-host-input-N`;
- replaces the fake repository `Z:/...` fixture with an explicit Windows-to-POSIX conversion fixture, so RED/ GREEN behavior is portable across Linux and Git Bash.

## Verification

- TDD RED: corrected fixture fails on pre-fix behavior with the exact bind-mount assertion;
- hybrid no-host-Python: PASS;
- Python-runtime packaging: PASS;
- MSYS Docker host/container paths: PASS;
- P09.4 deployment-index + readiness + hybrid secret scopes: 29 tests + 3 subtests PASS;
- dedicated Windows recovery root, restore-generation initialization, pre-upgrade ordering, Windows path budget: PASS;
- Ripwire quality delta: no gating regression;
- release validator progressed through P03.2 browser acceptance before the sandbox command window; every remaining release stage was run individually and passed.

## Package

`mreader-rc485-p12.2-source-test-b80c030.zip`

SHA-256: `afe1cd9030ee64602a6840714af54bff1e083f204e15bca1499d1324d1e26ebc`

Integrity: 960/960 committed Git files byte-exact, 967/967 package checksum entries verified, deterministic rebuild byte-identical.

Real Windows/Docker Desktop execution remains the host acceptance gate.

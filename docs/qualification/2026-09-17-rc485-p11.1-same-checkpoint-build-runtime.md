# RC4.85 P11.1 — same-checkpoint build/runtime qualification

Date: 2026-09-17 (qualification executed 2026-09-16 UTC)

## Scope and immutable checkpoint

P11.1 was executed from a detached qualification worktree at exact tracker/handoff checkpoint:

- branch authority: `sequential/p06.4-caller-cutover`
- qualification SHA: `65f0c2ebada66a653700e76025d2a5ba1a051f69` (`65f0c2e`)
- P10.2 behavior/source ancestor: `b15b417f8a28fed6b5e2e0c762adf628a8a4b4f0`
- P10.2 quality-evidence ancestor: `cad882604331631497b0039bfd6d6635d6b4a7df`
- forensic stash: `ffbcfe842c0e7f0910eae8ccf4fe64f822c01af4` (not applied/popped)
- detached qualification worktree: `/mnt/data/mreader-p111-65f0c2e`
- raw command logs: `/mnt/data/p111-qualification-65f0c2e/`

The canonical checkout and detached qualification worktree remained clean after all probes. No production source was changed to make a blocked gate green.

Before P11.1 opened, the P10.2 immutable and rolling Library packages were independently re-materialized and verified byte-identical at SHA-256 `b92276c156797e27389b3b9c286c0e88052576a5df7a2d24609717035457c6d5`, size 4,065,648 bytes, with internal checksums, 19 advertised bundle heads / 17 real refs, exact tracker/source/evidence/stash and clean restored checkouts. Therefore the P10.2 durability prerequisite is closed.

## Changed build surfaces

Diffing the current branch against `refs/heads/sequential-baseline` identifies the changed application build surfaces exercised here:

- Go: `services/catalog_go`, `services/notification_worker`, `services/outbox_relay`, `services/progress_go`, `services/reader_go`, `services/realtime_go`
- Python: `shared`, `services/image_service`, `services/scraper_service`
- TypeScript/Web: `frontend`, `services/social_ts`
- Android: `android`
- actual API harness: `tests/api`

## Runtime/toolchain identity

Host/container identity:

- Linux x86_64
- Node.js `v22.16.0`
- npm `10.9.2`
- Go `go1.23.2 linux/amd64`
- Python `3.13.5` at `/opt/pyvenv/bin/python`
- Java `21.0.11`
- no system Gradle
- no Docker CLI/socket, Podman, kubectl, `psql`, or `postgres`

All six changed Go modules declare `go 1.25.0`. `GOTOOLCHAIN=auto` attempted the required 1.25.0 toolchain and failed because `proxy.golang.org` DNS access is unavailable.

Both Node projects have no `node_modules` in the checkpoint. The npm cache contains no usable artifacts. Frontend has a committed `package-lock.json`; Social has exact direct dependency versions in `package.json` but no `package-lock.json`, so its transitive dependency graph is not checkpoint-locked. Offline restore failed for both projects.

The Python images declare Python 3.12.11 and exact requirements. The sandbox Python environment is not that image environment: among required packages, `asyncpg`, `redis`, `aio-pika`, `pyvips`, `selectolax`, `scrapling`, and API-harness `psycopg` are unavailable. Installed versions of several present packages also differ from the exact image pins. Outbound dependency restore is unavailable in this sandbox.

Android's wrapper script attempted to obtain Gradle 9.6.0 and failed because `services.gradle.org` could not resolve.

## Build and focused-test results

The machine-readable matrix is `docs/qualification/2026-09-17-rc485-p11.1-build-matrix.tsv`.

### Go services — BLOCKED before compilation/tests

Each changed Go service ran:

`GOTOOLCHAIN=local go test -mod=readonly ./...`

Each exited `1` at the version gate because Go 1.23.2 cannot build a module requiring Go 1.25.0. A representative exact-toolchain recovery attempt with `GOTOOLCHAIN=auto` also exited `1` because the Go 1.25.0 toolchain could not be downloaded. No Go unit test result is recorded as a pass.

### Web frontend — BLOCKED

- `npm run build` -> exit `127`; `vite` is unavailable because `node_modules` is absent.
- `npm run typecheck` -> exit `2`; the globally available compiler cannot resolve React/project modules/types. This is dependency-blocked evidence, not a TypeScript source failure verdict.
- `npm ci --offline --ignore-scripts` -> exit `1`, `ENOTCACHED`.

No Web production build is recorded as passed.

### Social TypeScript — BLOCKED

- `npm run build` -> exit `2` because project packages/types are unavailable.
- clean `npm ci --offline` cannot run because the checkpoint contains no `package-lock.json`.
- `npm install --offline --ignore-scripts --no-audit --no-fund` -> exit `1`, `ENOTCACHED`.

No Social production build is recorded as passed. The missing lockfile is recorded as build reproducibility debt, not silently repaired during qualification.

### Android — BLOCKED

`./gradlew --offline :app:assembleDebug` exited `6`. The project bootstrap attempted to fetch Gradle 9.6.0 and DNS resolution for `services.gradle.org` failed. No APK/Android compile result is recorded as a pass.

### Python services

Syntax/source compilation passes:

- `python -m compileall -q shared` -> exit `0`
- `python -m compileall -q services/image_service` -> exit `0`
- `python -m compileall -q services/scraper_service` -> exit `0`

Runtime/import gates remain blocked:

- Image Service import with the repository's actual shared-package path -> exit `1`, missing `redis`.
- Scraper import -> exit `1`, missing `asyncpg`.
- Scraper focused tests -> exit `2` during collection, missing `selectolax`.

These compile passes are not substituted for service runtime/build acceptance.

### Actual API tests — BLOCKED, no requests issued

The API harness default targets are Docker-internal gateways and PostgreSQL. This sandbox has no Docker/PostgreSQL runtime, `host.docker.internal` does not resolve, and `TEST_USER_BASE_URL`, `TEST_ADMIN_BASE_URL`, and `TEST_DATABASE_URL` are unset.

The safest non-mutating harness test was attempted:

`PYTHONPATH=tests/api python -m pytest -q tests/api/test_00_harness_safety.py`

It exited `4` while loading `conftest.py` because `psycopg` is missing. No HTTP/API request was issued, and no actual API gate is marked passed.

## P11.1 disposition

**P11.1 = BLOCKED VERIFICATION at exact checkpoint `65f0c2e`.**

The result is not evidence of an application regression. It is evidence that this sandbox cannot execute the required same-checkpoint build/runtime gate because the declared toolchains/dependencies/runtime are unavailable and cannot be restored from local caches or the blocked network.

To close P11.1, rerun this unchanged checkpoint in an authorized isolated environment providing:

1. Go 1.25.0;
2. frontend lockfile dependency install and Node build tooling;
3. Social dependencies (and, for reproducible checkpoint resolution, a reviewed lockfile policy/lock artifact);
4. Python 3.12.11 plus the exact pinned Image/Scraper/API requirements and native libvips runtime;
5. Gradle 9.6.0 / Android build environment;
6. Docker Desktop hybrid runtime or another explicitly authorized disposable equivalent with PostgreSQL, gateways and required stateful dependencies.

P11.2 restore rehearsals, P11.3 load/query/resource qualification and P11.4 independent review are not treated as passed by this report. Static/source checks from earlier phases remain useful evidence but are not replacements for the blocked P11.1 runtime gates.
